import { Fragment } from "react";
import Login from "../features/auth/component/login";

const LoginPage = () => {
  return (
    <Fragment>
      <Login></Login>
    </Fragment>
  );
};

export default LoginPage;
